{
  "name": "elektroserviszemlicka-astro",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "11",
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^4.5.18",
    "tailwindcss": "^3.4.19"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
